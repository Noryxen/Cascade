MUSE_forget_verbmem:
  handler: CompletionDataset
  args:
    hf_args:
      path: muse-bench/MUSE-${dataset_defaults.muse.data_split}
      name: "verbmem"
      split: "forget"
    prefix_key: "prompt"
    text_key: "gt"
    max_length: ${dataset_defaults.muse.max_length}
    insert_space: True
